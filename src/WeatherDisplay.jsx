import React from "react";

const WeatherDisplay = ({
	data: {
		city,
		time,
		country,
		region,
		temp,
		condition,
		wind,
		is_day,
		precip,
		alt_time,
		alt_temp,
		alt_wind,
		alt_precip,
	},
}) => {
	return <div></div>;
};

export default WeatherDisplay;
